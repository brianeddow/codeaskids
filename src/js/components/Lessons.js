import React, { Component } from "react";
import { COLORS } from "../../css/styles";
import styled from "styled-components";

class Lessons extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
    	<section>
	    	```
	    		Lessons:
	    		HTML/CSS/JS

	    		html: what is a tag/element
	    		html: boilerplate HTML
	    		html: what is a parent/child element
	    		html: what is a sibling element
	    		html: hyperlinks (download,phone,open-in-new-tab,ga)
	    		html: meta tags
	    		css: is it an acronym?
	    		css: what are tag/class/id selectors
	    		css: selector priority
	    		css: multiple/child selectors
	    		css: external stylesheets
	    		css: media query
	    		js: variable declaration, assignment, reassignment
	    		js: variable types
	    		js: scope: where is my var available
	    		js: what is the window
	    		js: what is the dom
	    		js: how can we access the dom
	    		js: external JS files
	    		js: external libraries


	    	```
	    </section>
    );
  }
}

export default Lessons;
